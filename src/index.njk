---
layout: default
title: Home
eleventyExcludeFromCollections: true
---
 

{% set cssFilename = "page-home.css" %}
{% include "add-css.njk" %}

{#
JSON-LD for:
- Site name: https://developers.google.com/search/docs/data-types/sitename
- Site links searchbox: https://developers.google.com/search/docs/data-types/sitelinks-searchbox
- Social Profile Links: https://developers.google.com/search/docs/data-types/social-profile-links
#}

<script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "{{websiteid.name}}",
    "url": "{{websiteid.url}}",
    "potentialAction": {
      "@type": "SearchAction",
      "target": "{{websiteid.url}}/archives/?query={search_term_string}",
      "query-input": "required name=search_term_string"
    },
    "sameAs": [{% for urll in websiteid.sameAs %} "{{ urll}}", {% endfor %}]
  }
</script>

<div class="page">

  <div class="wrap site__intro clearfix h-card">
    <img src="/assets/me/{{websiteid.image}}" alt="" class="logo portrait" />
    <p class="site__intro__name">Hi, I'm <a href="/about/"><strong class="p-name"> {{websiteid.name}}</strong></a>.</p>
    <p class="p-note">
  I am a <em> theoretical biologist</em> working at the crossroads of experimental biology, mathematics, and philosophy. I also work on the Anthropocene,
      among <a href="/tags/">many other interests</a>.
    </p>
  </div>



  {% from "macros/articleCard.njk" import articleCard %}
  {% from "macros/pubCard.njk" import pubCard %}

  <h2   class="wrap">Latest articles</h2>
  <div class="cards-list">
    {% for article in collections.latestPeerreviewed %}
      {{ pubCard(article,numbers.numbers2) }}
    {% endfor %}
  </div>
  
  <h2 class="wrap">Latest publications</h2>
  <div class="cards-list">
    {% for article in collections.publications %}
      {{ pubCard(article,numbers.numbers2)   }}
    {% endfor %}
  </div>
  
  <h2 class="wrap">Latest articles</h2>
  
  <ol class="cards-list">
    {% for article in collections.latestArticles %}
      {{ articleCard(article) }}
    {% endfor %}
  </ol>

  <h2 class="wrap">Older noteworthy articles</h2>
  <ol class="cards-list">
  {% for article in collections.promotedArticles %}
    {{ articleCard(article) }}
  {% endfor %}
  </ol>
  <p class="wrap more ">Read <a href="/articles/">all articles</a>…</p>

  <div class=" lazy "></div>
</div>



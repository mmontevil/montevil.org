---
layout: null
permalink: /feeds/talks.xml
isFeed: true
eleventyExcludeFromCollections: true
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{websiteid.name}} - Talks</title>
  <link href="{{ pkg.homepage }}/feeds/talks.xml" rel="self"/>
  <link href="{{ pkg.homepage }}/all/talks/"/>
  <link rel="hub" href="http://pubsubhubbub.superfeedr.com/" />
  <updated>{{ collections.talkssall | getNewestCollectionItemDate | dateToRfc3339 }}</updated>
  <id>{{ pkg.homepage }}/talks/</id>
  <author>
    <name>{{ pkg.author.name }}</name>
    <email>{{ pkg.author.email }}</email>
  </author>
  {% for entry in collections.talksall | limit(150) %}
    {% set absoluteEntryUrl %}{{ entry.url | url | absoluteUrl(pkg.homepage) }}{% endset %}
    <entry>
      <title> 💬{{ entry.data.title }}</title>
      <link href="{{ absoluteEntryUrl }}"/>
      <updated>{{ entry.data.orderDate |toDate }}</updated>
      <id>{{ absoluteEntryUrl }}</id>
      <content type="html">{{ entry.content  |  renderTransforms(post.data.page, metadata.base) }}</content>
    </entry>
  {% endfor %}
</feed>

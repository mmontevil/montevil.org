{% macro card(type = false, lang = 'en', surtitle = false, title = false, url = false, illustration = false, content = false, meta = false) %}
  <li class="card-list__item">
    <article class="wrap card {{ type or '' }} h-entry" lang="{{ lang }}">
      {% if illustration.src %}{% if not (""== illustration.src )  %}
        <img
          alt="{{ illustration.alt }}"
       {#   width="{{ illustration.width }}"
          height="{{ illustration.height }}"#}
          src="{{ illustration.src }}"
          class="card__illustration {% if illustration.classes %}{{ illustration.classes }}{% endif %}"
          crossorigin="anonymous" />
      {% endif %}{% endif %}
      {% if surtitle %}
        <p class="card__surtitle">{{ surtitle | safe }}</p>
      {% endif %}
      {% if title %}
        <h3 class="card__title p-name">
          <a href="{{ url }}" hreflang="{{ lang }}" class="u-url">{{ title | orphans | safe }}</a>
        </h3>
      {% endif %}
      {% if meta %}
        {{ meta | safe }}
      {% endif %}
      {% if content %}
          {{ content | safe }}
        
      {% endif %}
    </article>
  </li>
{% endmacro %}

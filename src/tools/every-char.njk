---
layout: default
title: Every characters
---

<h1>Every characters used on this site</h1>

{% from "macros/postCard.njk" import postCard %}

{% set every_char = '' %}
{% for article in collections.posts %}
  {% set article_chars %}{{ postCard(article) }}{{ article.templateContent }}{% endset %}
  {% set new_every_char %}{{ every_char }}{{ article_chars | striptags | split("") | uniq | join("") }}{% endset %}
  {% set every_char = new_every_char %}
{% endfor %}

{% from "macros/pubCard.njk" import pubCard %}
{% for article in collections.publication %}
  {% set article_chars %}{{ pubCard(article,numbers.number2) }}{{ article.templateContent }}{% endset %}
  {% set new_every_char %}{{ every_char }}{{ article_chars | striptags | split("") | uniq | join("") }}{% endset %}
  {% set every_char = new_every_char %}
{% endfor %}

{{ every_char | striptags | split("") | uniq | sort | join("") }}
